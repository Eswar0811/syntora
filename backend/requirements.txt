# ── Syntora Backend — Production (no ML/Whisper) ──────────────────────────────
# Use this file for free-tier Railway / Render / Fly.io deployments.
# Rule-based transliteration still works perfectly without torch/transformers.
# For full neural engines, use requirements-neural.txt on a machine with ≥2 GB RAM.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.11.3
requests>=2.32.0
urllib3>=2.2.0
python-dotenv>=1.0.1
python-multipart>=0.0.20
sse-starlette>=2.2.1
httpx>=0.27.0
