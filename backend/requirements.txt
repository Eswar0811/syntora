fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
requests>=2.31.0
urllib3>=2.0.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
sse-starlette>=1.6.5

# ── ML / Neural engines (optional — heavy, ~2 GB) ────────────────────────────
# Omit these for free-tier cloud deployment. Use requirements-prod.txt instead.
transformers>=4.40.0
torch>=2.2.0
sentencepiece>=0.2.0
protobuf>=4.25.0
datasets>=2.19.0
evaluate>=0.4.2
accelerate>=0.29.0
jiwer>=3.0.4

# ── Audio fallback (optional — requires system ffmpeg) ────────────────────────
openai-whisper>=20231117
